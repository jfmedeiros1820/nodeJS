<html>
    <body>
        <form method='post' action='/promotions'>
            <div>
                <input type="text" name="message"/>
            </div>
            <div>
                <select name="book[id]">
                    <%for(var i=0; i<list.length; i++){%>
                        <option value="<%=list[i].id%>" ><%=list[i].title%></option>
                    <%}%>
                </select>
            </div>
            <input type="submit" value="Save Promotion">
        </form>
    </body>
</html>